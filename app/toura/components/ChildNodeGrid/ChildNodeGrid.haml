%ul.component.child-node-grid
  :each c in children
    %li
      %a{ href : c.url }
        :if tablet
          .image{ dojoType : 'mulberry.ui.BackgroundImage', src : c.featuredImage.large.url, loadOnInit: true }
        :if phone
          .image{ dojoType : 'mulberry.ui.BackgroundImage', src : c.featuredImage.small.url, loadOnInit: true }
        %h3= c.name
