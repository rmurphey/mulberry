dojo.provide('client.stores.{{name}}');

mulberry.store('{{name}}', {
  /*
   * This creates a store at client.stores.{{name}}
   *
   * You can augment this store by adding properties and methods to this
   * object.
   */
});
