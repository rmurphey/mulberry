AnimalChooser:
  capabilities:
  - name: AnimalVoting
    components:
      - index:custom.AnimalChooser
      - index:custom.AnimalFavorites
  screens:
  - name: index
    regions:
      - type: row
        size: fixed
        scrollable: false
        components:
          - PageNav
      - type: row
        size: flex
        scrollable: true
        components:
          - custom.AnimalChooser
          - custom.AnimalFavorites
