user:
  screens:
    - name: index
      regions:
        - type: row
          size: fixed
          scrollable: false
          components:
            - PageNav

        - type: row
          size: flex
          scrollable: true
          components:
            - custom.Tweets
